{
  "name": "k3-logistics-v3",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run check:fonts && tsc && vite build",
    "preview": "vite preview",
    "check:fonts": "node scripts/check-font-floor.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.0.0"
  },
  "devDependencies": {
    "typescript": "7.0.2",
    "vite": "8.2.0"
  }
}
